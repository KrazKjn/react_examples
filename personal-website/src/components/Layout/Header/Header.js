import React from 'react';

const Header = () => {
    return (
        <header className="header">
            {/* Add navigation links if needed */}
        </header>
    );
};

export default Header;